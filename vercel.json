{
  "buildCommand": "npm run build",
  "installCommand": "npm install --force",
  "outputDirectory": "build",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api-system/:path*",
      "destination": "https://dev.blueledgers.com:4001/api-system/:path*"
    },
    {
      "source": "/((?!static|favicon|manifest|logo|asset-manifest).*)",
      "destination": "/index.html"
    }
  ]
}
